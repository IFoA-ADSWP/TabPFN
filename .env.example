# TabPFN API key — only needed if using Prior Labs hosted API (default is local model).
# Copy this file to .env (gitignored).
# Get a key at: https://ux.priorlabs.ai/api/keys

TABPFN_API_KEY=your_key_here
